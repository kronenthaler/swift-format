sudo: false

language: python
python:
    - "2.7"

# command to install dependencies
install: 
    - pip install .
    - pip install coveralls

# command to run tests
script: 
    - nosetests --with-coverage tests/*
#    - coverage run --source=. nosetests tests/*

after_success: coveralls
